const payPoints = (props) => {
  if (currentScore >= 10) {
  }
};

//logic for future use:
//if currentScore is >=10, when user clicks "Pay 10 points", increase value of y by 1, then add y to current value, then increase value of x by 1.
