import React from 'react'

const CurrentValue = (props) => {
    return (
        <div>
            <button onClick={props.onClick} type ='submit'>Play again?</button>
        </div>
    )
}
export default CurrentValue
